# Copy this file to .env and fill in real values. Never commit .env itself.

DEBUG=True
SECRET_KEY=nuttertools

# PostgreSQL
DB_NAME=talentsync_db
DB_USER=talentsync_user
DB_PASSWORD=2004amare
DB_HOST=localhost
DB_PORT=5433
DB_SCHEMA=talentsync

# Comma-separated origins allowed to call the API (no trailing slash)
CORS_ALLOWED_ORIGINS=http://localhost:5173

# Email (Phase 4 — leave console backend for now)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=sandbox.smtp.mailtrap.io
EMAIL_PORT=2525
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
